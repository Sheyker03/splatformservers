import React from 'react';
import {Paper} from "@mui/material";

const Footer = () => {
    return (
        <Paper square elevation={0} sx={{height: '10%', border: '1px solid black'}}>

        </Paper>
    );
};

export default Footer;